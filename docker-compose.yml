services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ega-mba-landing:latest
    container_name: ega-mba-app
    restart: always
    ports:
      - "127.0.0.1:3032:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
