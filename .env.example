# Optional. If set, every validated lead is POSTed as JSON to this URL by
# app/api/lead/route.ts. Leave unset in development — leads are logged instead.
# LEAD_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/000000/abcdef

# Add CRM credentials here when you wire up HubSpot / Zoho / SendGrid.
# HUBSPOT_PORTAL_ID=
# HUBSPOT_FORM_GUID=
